-module(hello_ros_erlang).

start() ->
    register(enode1_process, self()),
    net_kernel:connect('hello_ros_erlang_node@localhost'),
    loop().

loop() ->
    receive 
	Message ->
	    io:format("got message: ~p ~n", Message);
	_ ->
	    io:format("received unknown message")
    end.
